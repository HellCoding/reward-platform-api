spring:
  datasource:
    url: jdbc:mysql://mysql:3306/reward_platform?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: root

  data:
    redis:
      host: redis
      port: 6379
